name: qingjuan
description: 青卷：支持 Windows 本机/远程与 Android 扫码连接 PC 局域网后端的 Flutter 阅读客户端。
publish_to: none
version: 2.2.0+42

environment:
  sdk: ^3.5.0
  flutter: '>=3.44.2'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  desktop_drop: ^0.7.1
  flutter_tts: 4.2.5
  flutter_miuix: 1.1.1
  flutter_secure_storage: ^9.2.4
  file_selector: ^1.0.3
  fluent_ui: 4.9.2
  http: ^1.5.0
  crypto: ^3.0.6
  app_links: ^6.4.1
  mobile_scanner: ^7.2.0
  package_info_plus: ^8.3.1
  path: ^1.9.0
  path_provider: ^2.1.4
  qr_flutter: ^4.1.0
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1
  window_manager: 0.4.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  # fluent_ui 4.9.x 内部仍引用部分 Material 图标字体。
  uses-material-design: true
  assets:
    - assets/logo.png
